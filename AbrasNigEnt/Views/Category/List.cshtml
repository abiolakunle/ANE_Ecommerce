@model IEnumerable<Category>

<main id="category -list" class="body-part">
    <div>
        @foreach (var c in Model)
        {
            <h2>@c.CategoryName</h2>
            <p>@c.Description</p>
        }
    </div>
</main>